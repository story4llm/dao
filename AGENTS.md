# DAO Agent 工作规范

本文件是仓库内所有 AI Agent 的唯一主规范。`CLAUDE.md` 与 `.github/copilot-instructions.md` 只能作为入口，不得复制或改写本文件的规则。

## 1. 使命

构建“东方趋势认知 + 现代概率预测 + AI Agent + 金融数据”的黄金趋势分析辅助系统。

系统的目标不是给出确定价格或代替用户交易，而是持续回答市场状态、主导力量、趋势阶段、情景概率、关键变化条件和风险边界。

项目的首要研究对象是“某一时点上可证伪、可修正的市场信念状态”，其核心载体是 Market Cognition Frame。代码、Agent 数量和页面只是获取证据、形成认知、回放与评分的手段，不是项目目标。

第一位用户仍是内部趋势研究者。第一研究轨验证 XAU/USD 日线与 4 小时级别的每日离线认知闭环；第二研究轨支持单一明确月份的 COMEX GC 黄金期货，并与现货保持独立契约。未完成金标准、基线和评估前，不扩张到实时平台、连续合约自动换月或外部交易产品。

项目的实际运行模式是：AI 读取 GitHub Harness 与历史认知状态；prepare 运行器从配置的白名单官方 HTTPS 地址自动生成 point-in-time 宏观/事件快照，并读取调用者一次性授权后保留在私有证据包中的账户级数据，生成新的认知帧、预测合同与观点变化记录。GitHub 不是行情数据库；许可受限原始数据和凭据必须留在私有运行环境。

## 2. 不可违反的原则

1. **观变，不断言。** 使用条件化、概率化语言，不把模型输出写成事实。
2. **传统思想与计算引擎分层。** “形、势、机、时、位”可以定义领域本体；卦象、术数或文化类比不能直接生成买卖信号。
3. **证据先于叙事。** 先形成结构化事实、模型输出和冲突，再由 LLM 解释。
4. **时间必须可追溯。** 每个结论同时记录事件时间、数据可获得时间、`as_of` 与 `data_cutoff`。
5. **允许弃权。** 数据过期、证据不足、模型冲突或分布外状态下，输出 `abstain`。
6. **风险守门优先。** 风险 Agent 可以阻止建议生成；解释 Agent 不能绕过风险结论。
7. **不自动交易。** 未经新的架构决策与专项安全评审，仓库不得接入实盘下单。
8. **不构成投资建议。** 所有面向用户的输出必须保留研究与决策辅助边界。

## 3. 开始任务前

始终阅读：

- `README.md`
- `state/PROJECT_STATE.md`
- 当前 `tasks/` 下的任务文件；若不存在，从 `tasks/TEMPLATE.md` 新建

再按 `docs/harness/CONTEXT_MAP.md` 加载最少必要上下文。不要为了“了解项目”一次性读取全部文件。

执行每日趋势认知时，还必须读取：

- `prompts/daily-cognition-run-v0.3.md`
- `docs/harness/DAILY_RUNBOOK.md`
- `docs/data/data-source-qualification-matrix-v0.2.md`

若任务改变产品目标、关键本体、公共数据契约、评估口径或安全边界，必须先新建 ADR，不能只改代码。

## 4. 标准工作循环

1. **Frame**：写清问题、用户价值、范围、非目标、风险。
2. **Load**：按上下文地图读取相关事实与约束。
3. **Plan**：列出实施步骤、验证方法和需要人工确认的决策。
4. **Implement**：只处理本任务；不顺手扩张到相邻功能。
5. **Challenge**：主动构造反方解释、失败模式和时间穿越检查。
6. **Validate**：运行 `make validate` 及本任务相关测试。
7. **Record**：更新任务结果、`PROJECT_STATE.md`；有决策时更新日志或 ADR。
8. **Handoff**：说明完成内容、证据、验证结果、残余风险和下一步。

一个会话、分支和 PR 原则上只对应一个主题。无关工作建立新任务。

## 5. 信息与证据规则

证据分为四类：

- `classical`：经典原文与可靠注本，只支持思想解释。
- `modern`：论文、教材与可复现研究，支持方法论与模型选择。
- `official`：央行、统计机构、交易所、监管机构等一手资料。
- `market`：有明确许可、时间戳和字段定义的行情或商业数据。

研究产物必须区分：

- **Observation**：来源直接支持的观察。
- **Inference**：本项目基于观察做出的推断。
- **Hypothesis**：尚待数据检验的假设。
- **Decision**：已选择的工程方案及理由。

引用要求见 `docs/harness/RESEARCH_POLICY.md`。数据和模型输出不得由 LLM 补全或猜测。

数据资格和私有留存边界以 `docs/data/data-source-qualification-matrix-v0.2.md` 为准。公开网页、图表和截图不能自动升级为 Q1 行情证据。禁止使用来源条款不允许进入 AI 运行时的数据。

## 6. 市场认知输出契约

每个可发布判断至少包含：

- Cognition Run 的模式、状态和数据门结果
- 标的、时间尺度、`as_of`、`data_cutoff`
- 方向、生命周期、稳定性
- “形、势、机、时、位”的现代定义值
- 主要支持证据与反方证据
- 至少两个互斥情景及其概率
- 关键失效条件
- 风险等级与研究姿态
- 数据缺口、模型分歧和是否弃权
- 生成该判断的模型、规则和版本

对应结构以 `schemas/market-cognition-frame.schema.json` 为准。概率和必须通过程序校验，不得只依赖文本审查。

## 7. 工程规则

- 先定义契约，再实现生产者和消费者。
- 纯计算、状态识别、概率融合与 LLM 解释必须解耦。
- 所有特征应说明单位、频率、来源、可获得时间和缺失值策略。
- 回测必须采用 point-in-time 数据和时间顺序切分，不得随机打乱金融时序。
- 任何“势”指标都只是候选特征；必须与朴素基线比较，通过样本外验证后才可保留。
- Prompt、模型版本、特征版本、数据快照和输出必须可追溯。
- 不在仓库提交密钥、付费数据、许可受限原始行情、原始大文件、个人账户信息或经纪商凭据。
- `certified` 运行必须附带私有 evidence manifest、原始响应哈希和冻结基线；GC 还必须冻结单一合约身份、交割生命周期和五-session 日历哈希。任一核心门失败时降级或阻断，不能从网络搜索补行情。
- AI 可以形成竞争假设与版本化预测，但不得修改程序计算的 ATR、冻结基线或到期结果，也不得用均匀概率伪装缺失能力。
- 优先小改动和可逆决策，避免为尚未验证的需求设计庞大平台。

## 8. 完成定义

只有同时满足以下条件，任务才可标记完成：

- 范围内产物已经实现，且非目标没有被悄悄纳入。
- 相关契约、文档与示例同步更新。
- 自动校验通过；无法自动验证的部分有明确人工检查记录。
- 已检查前视偏差、数据陈旧、概率不一致、确定性措辞和证据缺失。
- `state/PROJECT_STATE.md` 与任务文件反映最新状态。
- 交付说明包含验证结果和残余风险。

## 9. 禁止事项

- 不把“物极必反”等文化命题翻译为无条件反转信号。
- 不用单一技术指标替代市场状态判断。
- 不让多个 Agent 重复同一证据后错误地提高置信度。
- 不隐去互相冲突的证据或只展示成功案例。
- 不用修订后的宏观数据模拟当时决策。
- 不把公开网页可见性等同于可用于 AI、可回放或可再分发的数据许可。
- 不把回测收益当作真实交易承诺。
- 不在没有失效条件时输出行动建议。
